# SDE_intern_assignment


using docker
1. install Docker
2. clone this repo
   cd SDE_intern_assignment
   cd flask_mongo_crud
3. Build the Docker Image: docker-compose build
4. Run the Docker Container: docker-compose up





